// using System.Collections.Generic;
// using System.Threading.Tasks;
// using mbt.webapi.Configuration;
// using mbt.webapi.Domain.Entities;
//
// namespace mbt.webapi.Services.Interfaces;
//
// public interface ISmtpService : IBaseService
// {
//     ISmtpSettings Settings { get; set; }
//     void Send(IEnumerable<string> to, string subject, string body);
//     Task SendAsync(IEnumerable<string> to, string subject, string body);
//     Task SendAsync(IEnumerable<MailEntity> mails);
// }
